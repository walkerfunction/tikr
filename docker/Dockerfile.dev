# Development image — pure Go, no CGo dependencies.
FROM golang:1.24-bookworm

WORKDIR /app

# Cache Go dependencies first
COPY go.mod go.sum ./
RUN go mod download

# Copy everything
COPY . .

# Default: run tests
CMD ["go", "test", "-race", "-v", "./pkg/..."]
